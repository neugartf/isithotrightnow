[project]
name = "lambda"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.38.41",
    "requests>=2.32.4"
]

[dependency-groups]
dev = [
    "ruff>=0.12.0",
    "scaleway-functions-python>=0.2.3",
]
